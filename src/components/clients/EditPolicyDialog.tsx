
// Since this is a read-only file, we'll need to create a new wrapper component that exports the EditPolicyDialog

// Re-export the EditPolicyDialog component
export { default as EditPolicyDialog } from "./EditPolicyDialogWrapper";

